spring:
  application:
    name: graphql-example

  graphql:
    graphiql:
      path: /graphiql
      enabled: true

  datasource:
    url: jdbc:mysql://localhost:3306/testdb
    username: root
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
